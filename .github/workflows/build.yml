name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: test1
      run: ls -a
    - name: configure
      run: cd Project/GCC_LINUX
    - name: test2
      run: ls -a
    - name: make
      run: make
